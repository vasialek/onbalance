@if(Request.IsAuthenticated) {
    <text>Welcome <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Log out", "logout", "user") ]</text>
}
else {
    @:[ @Html.ActionLink(OnBalance.MyMessages.ButtonText.Login, "login", "user") ]
}
